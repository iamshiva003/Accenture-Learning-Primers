-- Create an Index with the name 'stuindex' for the student table in the student_name column.
-- (Note: Use ';' to terminate your query before compilation and evaluation.)
-- Refer to the given schema.

CREATE INDEX stuindex ON student (student_name);